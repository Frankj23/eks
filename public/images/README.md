# Images

Empty by design.

The original Stitch export hotlinked 31 images from `lh3.googleusercontent.com`.
Those are temporary URLs and **will break**.

Download each one, convert to WebP, save it here, and reference it as
`/images/filename.webp` with a real, descriptive `alt` attribute.
