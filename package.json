{
  "name": "eks-tech",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pages:dev": "npm run build && wrangler pages dev dist",
    "d1:migrate:local": "wrangler d1 execute eks-properties --local --file=migrations/0001_properties.sql && wrangler d1 execute eks-properties --local --file=migrations/0002_engineering_projects.sql",
    "d1:migrate:remote": "wrangler d1 execute eks-properties --remote --file=migrations/0001_properties.sql && wrangler d1 execute eks-properties --remote --file=migrations/0002_engineering_projects.sql"
  },
  "dependencies": {
  "@astrojs/sitemap": "^3.7.3",
  "@tailwindcss/vite": "^4.3.3",
  "astro": "^7.2.0",
  "sharp": "^0.35.3",
  "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.29.0"
  }
}
